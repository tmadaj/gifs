import styled from 'styled-components';

export const Cell = styled.li`
  list-style: none;
  overflow: hidden;
  aspect-ratio: 1/1;
`;

export const Gif = styled.img`
  width: 100%;
  height: 100%;
  object-fit: cover;
`;
